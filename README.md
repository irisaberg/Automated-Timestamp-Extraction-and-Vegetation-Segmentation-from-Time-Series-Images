Description:

Sam_segmentation:
Used to mask and segment parts of an image. The images could be from a timeseries captured by a fixed camera

Testing:
Used to extract timestamp from image and classify the digits inside

Train:
Used to train a CNN model to classify digits

digit_cnn:
The trained CNN model?
